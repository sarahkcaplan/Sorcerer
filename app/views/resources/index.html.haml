%h1="Main Resource Page"

-resources.each do |resource|
  =link_to resource.title resources_path(resource)

  = link_to "Add New Resource", new_resource_path
  = link_to("Show, {}, :class => 'action show')

